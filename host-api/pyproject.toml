[project]
name = "host-api"
version = "0.1.0"
description = "Internal host API for privileged container operation"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.136.1",
]

